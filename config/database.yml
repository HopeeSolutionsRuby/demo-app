default: &default
  adapter: mysql2
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000
  host: localhost

development:
  <<: *default
  database: school_development
  username: <%= ENV.fetch("DB_USERNAME") { 'root' } %>
  password: <%= ENV.fetch("DB_PASSWORD") { 'Matkhau123' } %>

# test:
#   <<: *default
#   database: beauty_care_test
#   username: <%= ENV.fetch("DB_USERNAME") { 'root' } %>
#   password: <%= ENV.fetch("DB_PASSWORD") { 'Matkhau123' } %>

# production:
#   <<: *default
#   database: beauty_care_production
#   username: <%= ENV.fetch("DB_USERNAME") { 'root' } %>
#   password: <%= ENV.fetch("DB_PASSWORD") { 'Matkhau123' } %>
